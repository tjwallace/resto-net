!!!
%html
  %head
    %title= content_for?(:title) ? content_for(:title) : "Inspections Ouvert"
    - %w(reset text 960 main).each do |css_file|
      = stylesheet_link_tag css_file
    = javascript_include_tag :defaults
    = content_for :head
    = csrf_meta_tag
  %body
    #main.container_12
      #header.grid_12
        %h1 inspections ouvert
        %h2 sub title goes here
      %div.clear
      #content
        = yield
      %div.clear
      #footer.grid_12
        this is the footer
      %div.clear
